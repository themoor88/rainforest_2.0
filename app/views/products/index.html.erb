<h1>All of the Products</h1>
<%= link_to "Sign Up", new_user_path %> <%= link_to "Log In", new_session_path %> </br>
<% @products. each do |product|%>
  </br><a href = "<%= product_path(product) %>"<p><%= product.name %>, <%= product.description %></p></a>
<% end %>
